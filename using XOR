APPROACH:using XOR
          we can find XOR of all numbers,which will be the only element occuring once.



#include<iostream>
 using namespace std;
 int main()
 {
     int arr[]={1,2,1,3,2};
     int n=sizeof(arr)/sizeof(arr[0]);
     int ans=0;
     for(int i=0;i<n;i++)
     {
         ans=ans^arr[i];
     }
     cout<<ans<<endl;
     return 0;
 }
